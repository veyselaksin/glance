This project uses Graphify for codebase knowledge mapping.

- ALWAYS execute `graphify . --code-only` via background terminal tool to refresh the context before working on any major task, refactoring, or feature implementations.
- Once refreshed, read and refer to `graphify-out/graph.json` to understand the system architecture and function calls.